# 合同管理系统 - 快速启动指南

## 🚀 一键启动（没学过前端也能快速运行）

### 第一步：安装 Node.js

1. 访问 https://nodejs.org/
2. 下载并安装 LTS 版本（推荐 18.x 或更高版本）
3. 安装完成后，打开 PowerShell 或命令提示符，输入以下命令验证：
   ```bash
   node -v
   npm -v
   ```
   如果显示版本号，说明安装成功。

### 第二步：配置 npm 镜像（推荐）

**国内用户必看**：npm 默认源速度很慢，建议换成淘宝镜像。

**方法 1：临时使用淘宝镜像（推荐新手）**

```bash
npm install --registry=https://registry.npmmirror.com
```

**方法 2：永久切换到淘宝镜像**

```bash
npm config set registry https://registry.npmmirror.com
```

**方法 3：使用 cnpm（可选）**

```bash
# 安装 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com

# 之后用 cnpm 代替 npm
cnpm install
```

**恢复官方源（如果需要）**

```bash
npm config set registry https://registry.npmjs.org
```

### 第三步：安装项目依赖

1. 打开 PowerShell 或命令提示符
2. 进入项目目录：
   ```bash
   cd c:\Users\DamSu\Desktop\code\contract_fronted
   ```
3. 安装依赖（使用淘宝镜像）：
   ```bash
   npm install --registry=https://registry.npmmirror.com
   ```
   或者如果已永久切换镜像：
   ```bash
   npm install
   ```

### 第四步：配置后端地址（可选）

如果你的后端不是运行在 `http://localhost:8080`，请修改 `.env.local` 文件：

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:你的端口号
```

### 第五步：启动前端服务

在项目目录下运行：

```bash
npm run dev
```

看到以下信息说明启动成功：

```
✓ Ready in 2.3s
○ Local:        http://localhost:3000
```

### 第六步：访问应用

打开浏览器，访问：http://localhost:3000

---

## 📋 完整操作流程

```bash
# 1. 进入项目目录
cd c:\Users\DamSu\Desktop\code\contract_fronted

# 2. 安装依赖（只需要运行一次，使用淘宝镜像加速）
npm install --registry=https://registry.npmmirror.com

# 3. 启动开发服务器
npm run dev

# 4. 打开浏览器访问 http://localhost:3000
```

---

## 🔧 常见问题

### 问题 1：npm 命令找不到

**解决方法**：

1. 确保已安装 Node.js
2. 重启 PowerShell 或命令提示符
3. 如果还是不行，重启电脑

### 问题 2：npm install 很慢或卡住

**解决方法**：
使用淘宝镜像：

```bash
npm install --registry=https://registry.npmmirror.com
```

或永久切换镜像：

```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 问题 3：npm install 报错

**解决方法**：

1. 删除 `node_modules` 文件夹（如果存在）
2. 删除 `package-lock.json` 文件（如果存在）
3. 使用淘宝镜像重新安装：
   ```bash
   npm install --registry=https://registry.npmmirror.com
   ```

### 问题 4：端口 3000 被占用

**解决方法**：
使用其他端口启动：

```bash
npm run dev -- -p 3001
```

然后访问 http://localhost:3001

### 问题 5：页面打开了，但是没有数据

**解决方法**：

1. 确保后端 Spring Boot 服务已经启动
2. 检查 `.env.local` 中的后端地址是否正确
3. 打开浏览器开发者工具（按 F12），查看 Console 和 Network 标签的错误信息

### 问题 6：出现跨域错误（CORS）

**解决方法**：
需要在后端 Spring Boot 项目中添加 CORS 配置。

在后端项目创建配置类：

```java
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOrigins("http://localhost:3000")  // 前端地址
                .allowedMethods("GET", "POST", "PUT", "DELETE", "PATCH")
                .allowedHeaders("*")
                .allowCredentials(true);
    }
}
```

---

## 📖 页面说明

### 1. 首页 (http://localhost:3000)

- 简洁的导航页面
- 点击"合同管理"进入合同管理页面
- 点击"客户管理"进入客户管理页面

### 2. 合同管理页面 (http://localhost:3000/contracts)

- 查看所有合同列表
- 搜索合同（按编号、名称）
- 筛选合同（按状态、客户）
- 新增/编辑/删除合同

### 3. 客户管理页面 (http://localhost:3000/clients)

- 查看所有客户列表
- 搜索客户（按名称、联系人）
- 新增/编辑/删除客户

---

## 🎯 下一步

1. **启动后端服务**：确保 Spring Boot 后端已启动
2. **测试功能**：尝试添加客户和合同
3. **查看数据**：通过搜索和筛选功能查看数据

---

## 💡 提示

- 前端启动后会自动在浏览器中打开
- 修改代码后保存，浏览器会自动刷新（热更新）
- 如果遇到问题，先查看浏览器开发者工具（F12）的 Console 和 Network 标签
- 停止服务：在命令行中按 `Ctrl + C`

---

**祝你使用愉快！ 🎉**
