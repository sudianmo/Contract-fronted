# 合同管理系统 - 项目说明

## 📋 项目概览

这是一个基于 **Next.js 14** + **React 18** + **TypeScript** + **Ant Design 5** 构建的合同管理系统前端项目。

采用现代化的技术栈和简约的设计风格，支持合同和客户的完整 CRUD 操作。

---

## 🎯 核心功能

### ✅ 已实现功能

1. **合同管理**

   - 合同列表展示（分页）
   - 关键字搜索（合同编号、名称）
   - 多条件筛选（状态、客户）
   - 新增/编辑/删除合同
   - 表单验证

2. **客户管理**

   - 客户列表展示（分页）
   - 关键字搜索（客户名称、联系人）
   - 新增/编辑/删除客户
   - 表单验证（手机号、邮箱）

3. **导航界面**
   - 美观的首页导航
   - 快速访问各功能模块

---

## 📂 项目结构详解

```
contract_fronted/
│
├── app/                          # Next.js App Router 应用目录
│   ├── page.tsx                 # 首页 - 导航页面
│   ├── layout.tsx               # 根布局 - 全局布局配置
│   ├── globals.css              # 全局样式
│   │
│   ├── contracts/               # 合同管理模块
│   │   └── page.tsx            # 合同列表页面
│   │
│   └── clients/                 # 客户管理模块
│       └── page.tsx            # 客户列表页面
│
├── components/                   # React 组件目录
│   ├── ContractList.tsx         # 合同列表组件
│   └── ClientList.tsx           # 客户列表组件
│
├── services/                     # API 服务层
│   ├── contractService.ts       # 合同相关 API 封装
│   └── clientService.ts         # 客户相关 API 封装
│
├── types/                        # TypeScript 类型定义
│   └── index.ts                 # 通用类型定义
│
├── utils/                        # 工具函数
│   └── request.ts               # Axios 请求封装（拦截器）
│
├── .env.local                   # 环境变量（后端地址配置）
├── .gitignore                   # Git 忽略文件
├── package.json                 # 项目依赖配置
├── tsconfig.json                # TypeScript 配置
├── next.config.js               # Next.js 配置
│
├── README.md                    # 项目文档（技术文档）
└── 快速启动指南.md              # 快速启动指南（新手友好）
```

---

## 🔌 API 接口对接

### 后端接口约定

**统一响应格式：**

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

### RESTful API 列表

#### 合同接口

| HTTP 方法 | 路径                  | 说明                      |
| --------- | --------------------- | ------------------------- |
| GET       | `/api/contracts`      | 获取合同列表（分页+筛选） |
| GET       | `/api/contracts/{id}` | 获取单个合同详情          |
| POST      | `/api/contracts`      | 创建新合同                |
| PUT       | `/api/contracts/{id}` | 完整更新合同              |
| PATCH     | `/api/contracts/{id}` | 部分更新合同              |
| DELETE    | `/api/contracts/{id}` | 删除合同                  |

**查询参数示例：**

```
GET /api/contracts?pageNum=1&pageSize=10&keyword=软件&status=生效&clientId=1
```

#### 客户接口

| HTTP 方法 | 路径                          | 说明                 |
| --------- | ----------------------------- | -------------------- |
| GET       | `/api/clients`                | 获取客户列表（分页） |
| GET       | `/api/clients/{id}`           | 获取单个客户详情     |
| POST      | `/api/clients`                | 创建新客户           |
| PUT       | `/api/clients/{id}`           | 完整更新客户         |
| PATCH     | `/api/clients/{id}`           | 部分更新客户         |
| DELETE    | `/api/clients/{id}`           | 删除客户             |
| GET       | `/api/clients/{id}/contracts` | 获取客户的所有合同   |

**查询参数示例：**

```
GET /api/clients?pageNum=1&pageSize=10&keyword=阿里巴巴
```

---

## 🛠️ 技术栈说明

### 前端框架

- **Next.js 14**: React 全栈框架，支持 App Router
- **React 18**: 前端 UI 库
- **TypeScript**: 类型安全的 JavaScript

### UI 组件库

- **Ant Design 5**: 企业级 UI 设计语言和 React 组件库
- **@ant-design/icons**: Ant Design 图标库
- **@ant-design/nextjs-registry**: Ant Design 的 Next.js 集成

### 数据请求

- **Axios**: Promise 基于的 HTTP 客户端
- **Day.js**: 轻量级日期处理库

---

## 🎨 设计理念

### 1. 简约设计

- 使用 Ant Design 默认主题
- 页面布局清晰，易于理解
- 操作流程简单直观

### 2. 响应式布局

- 表格支持横向滚动
- 弹窗自适应屏幕大小
- 移动端友好

### 3. 用户体验

- 实时表单验证
- 友好的错误提示
- 加载状态反馈
- 删除前确认

---

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置后端地址

修改 `.env.local` 文件：

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
```

### 3. 启动开发服务器

```bash
npm run dev
```

### 4. 访问应用

打开浏览器访问：http://localhost:3000

---

## 📝 开发指南

### 添加新模块

假设要添加"供应商管理"模块：

1. **定义类型** (`types/index.ts`)

   ```typescript
   export interface Supplier {
     id?: number;
     name: string;
     // ...其他字段
   }
   ```

2. **创建服务** (`services/supplierService.ts`)

   ```typescript
   import request from "@/utils/request";

   export const getSupplierList = (params) => {
     return request.get("/api/suppliers", { params });
   };
   // ...其他 API
   ```

3. **创建组件** (`components/SupplierList.tsx`)

   ```typescript
   // 参考 ContractList.tsx 或 ClientList.tsx
   ```

4. **创建页面** (`app/suppliers/page.tsx`)

   ```typescript
   import SupplierList from "@/components/SupplierList";

   export default function SuppliersPage() {
     return <SupplierList />;
   }
   ```

### 修改样式

- 全局样式：编辑 `app/globals.css`
- 组件样式：使用 inline style 或添加 CSS Module

### 调试技巧

1. **浏览器开发者工具**（F12）

   - Console：查看日志和错误
   - Network：查看 API 请求
   - React DevTools：调试组件

2. **VSCode 调试**
   - 在代码中打 `console.log()`
   - 使用 VSCode 断点调试

---

## 🔧 配置说明

### 环境变量

`.env.local` 文件用于配置环境变量：

```env
# 后端 API 地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
```

**注意**：以 `NEXT_PUBLIC_` 开头的变量会暴露到浏览器端。

### TypeScript 配置

`tsconfig.json` 配置了路径别名：

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

可以使用 `@/` 代替相对路径，例如：

```typescript
import request from "@/utils/request"; // 而不是 '../../../utils/request'
```

---

## 📦 构建部署

### 开发环境

```bash
npm run dev
```

### 生产构建

```bash
# 构建
npm run build

# 启动生产服务器
npm run start
```

### 部署到 Vercel

1. 推送代码到 GitHub
2. 在 Vercel 中导入项目
3. 配置环境变量 `NEXT_PUBLIC_API_BASE_URL`
4. 自动部署

---

## 🐛 常见问题

### 1. CORS 跨域问题

**现象**：浏览器控制台提示跨域错误

**解决**：在后端添加 CORS 配置（参考"快速启动指南.md"）

### 2. 数据加载失败

**排查步骤**：

1. 检查后端是否启动
2. 检查 `.env.local` 配置
3. 查看浏览器 Network 标签的请求状态
4. 查看后端日志

### 3. TypeScript 报错

**解决**：

1. 确保安装了所有依赖：`npm install`
2. 重启 VSCode
3. 检查类型定义是否正确

---

## 📚 学习资源

- **Next.js**: https://nextjs.org/docs
- **React**: https://react.dev/
- **Ant Design**: https://ant.design/
- **TypeScript**: https://www.typescriptlang.org/
- **Axios**: https://axios-http.com/

---

## 📄 许可

MIT License

---

**有问题？** 请查看"快速启动指南.md"或提交 Issue。
